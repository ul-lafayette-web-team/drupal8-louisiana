{# content.field_tfs_cost_items #} 

{% if content.field_tfs_cost_items[0] %}
    {% for key, item in content.field_tfs_cost_items if key|first != '#' %}
      {% if loop.first %}
        {{ item }}



      {% else %}
        {{ item }}
      {% endif %}
    {% endfor %}
  </ul>
{% endif %}


<div class="grid-x costs--subtotal">
<div class="cell small-12 medium-8 large-9">
  <div class="costs__textrow">
    <div class="costs__name">{{ content.field_tfs_total_name }}</div>
    <div class="costs__amount">{{ content.field_tfs_total_number }}</div>
  </div>      
</div>
</div>
{{ content.field_tfs_aid_items }}
